import { style } from 'typestyle';

const styles = {
    dateCard: style({
        marginTop: '16px',
        userSelect: 'none',
    }),
    backButton: style({
        marginTop: '24px',
    }),
};

export default styles;